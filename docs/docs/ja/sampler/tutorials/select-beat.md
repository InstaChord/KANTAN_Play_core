# ビートを選ぶ

ビートは、演奏の速さとLoopの長さを決める土台です。内蔵Pattern、SDカードのWAV / MP3 / MIDI、またはサンプラーパッドの音を使えます。

最初は内蔵Patternから選ぶのがおすすめです。

## 1. BEATパートを選ぶ

1. 右側の下のダイヤルを回します
2. パート選択から`BEAT`を選びます
3. 下のダイヤルを押してメニューを開きます
4. `Beat` → `Select Beat`へ進みます

<!-- IMAGE BEAT-01: BeatメニューのSelect Beatが選択されている画面。 -->

## 2. ビートの場所を選ぶ

| 選択肢 | 内容 |
|---|---|
| `Built-in` | Pop、Rock、House、Hip Hop、Disco、Breakなどの内蔵Pattern |
| `SD Card` | SDカードに保存したWAV、MP3、MID、MIDI |
| `Sampler Pad` | 現在のサンプラーパッドを独立したオーディオビートにする |

初めての場合は`Built-in`を選びます。

## 3. 試聴して決める

1. 下のダイヤルを回して候補を選びます
2. `Fn1`でPreviewします
3. 別の候補を聴く場合は、ダイヤルを回してからもう一度`Fn1`を押します
4. 気に入ったビートで`OK`を押します

Patternは元のTempoで1周、WAV / MP3は短い範囲がPreviewされます。Previewだけでは現在のProjectは変更されません。

<!-- IMAGE BEAT-02: Built-in Beat一覧とFn1 Preview表示。 -->

## Rec済みの演奏がある場合

ビートを変更すると、現在のRecをどう扱うか選ぶ画面が表示されることがあります。

| 選択肢 | 結果 |
|---|---|
| `Follow New Beat` | Recした演奏位置を、新しいビートの長さに合わせる |
| `Keep Current Tempo` | 現在のLoop長を保ち、ビート側を現在の速さへ合わせる |
| `Clear Rec` | Recした演奏を消し、新しいビートを基準にする |

迷った場合は`Follow New Beat`を選びます。今の演奏を消したくない場合は`Clear Rec`を選ばないでください。

## ビートの音色（ドラムキット）を変える { #change-beat-kit }

パターンビートでは、`Beat` → `Select Kit`からドラム音色を選べます。

- `Acoustic`
- `Dance`
- `Chiptune`

PatternとTempoを変えずに、ドラムの音色だけを交換できます。

## ビートの音量と繰り返し

ビートメニューでは次も調整できます。

- `Beat Volume`：ビートの音量
- `Beat Repeat`：ビートを1回、2回、4回繰り返してLoopの長さにする

ビート Repeatを変更すると、Recの基準となるLoop長も変わります。

[このビートへ演奏をRecする](record-loop.md){ .md-button .md-button--primary }
